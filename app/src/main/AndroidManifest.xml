<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:name=".workmanager.DownloadApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LearningNew">
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <activity
            android:name=".workmanager.WorkManagerActivity"
            android:exported="true"
            android:theme="@style/Theme.LearningNew">
        </activity>
        <activity
            android:name=".ComposeOptimizationActivity"
            android:exported="true"
            android:theme="@style/Theme.LearningNew">
        </activity>
        <activity
            android:name=".navigation.NavigationActivity"
            android:exported="true"
            android:theme="@style/Theme.LearningNew">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".contactsroom.ContactsActivity"
            android:exported="true"
            android:theme="@style/Theme.LearningNew">


        </activity>
    </application>

</manifest>